# Dowell — Winter Arc Checker (SPA)

Мини-приложение для контроля дисциплины: задачи на месяц, ежедневный ввод результатов, подсчёт баллов и прогресса.

## Стек
- Frontend: Vue 3 + Vite + Vue Router
- Backend: Python (FastAPI)
- DB: PostgreSQL (Docker)
- Docker Compose: frontend + backend + db
